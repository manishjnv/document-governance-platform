# Pending-work runbook (2026-09-12) — one prompt per session, run in order

Each block below is a complete prompt for a fresh Claude Code session opened at
`E:\code\DocumentGovernancePlatform`. Run them top to bottom; each assumes the
previous one landed. Sessions 2 and 3 are already written in their own files
and are only pointed to here. Anything marked **you** is a decision or input
only the owner can give.

| # | Session | Prompt | Depends on |
|---|---|---|---|
| 1 | Housekeeping commits (privacy page, Sentinel workbook, session docs) | below | nothing |
| 2 | Homepage branding + SEO build | `HOMEPAGE_BRANDING_SEO_PROMPT.md` | 1 |
| 3 | Content phase (blog posts, compare page, licence loose ends) | `CONTENT_PHASE_BLOG_COMPARE_PROMPT.md` | 2 |
| 4 | Push, deploy, smoke test, GSC resubmit | below | 2, 3, **your go** |
| 5 | Core Web Vitals re-check + templates lead magnet | below | 4 |
| 6 | Legal severity calibration pack (prep for the SME) | below | nothing (parallel OK) |
| 7 | Sentinel workbook first live test | below | **you**: a Sentinel workspace |
| — | Partner Center verification, Splunk live pull, case study | no prompt | **you**: external parties |

---

## Session 1 — Housekeeping commits

```
Read CLAUDE.md, then `git status --short`. The working tree holds four
unrelated pieces of finished work that need separate commits. Do not push.

Context: the privacy page and footer link were written 2026-08-20 and never
committed. The Sentinel workbook is Phase A of
docs/planning/SENTINEL_CONTENT_HUB_ADDON_PLAN.md, generated by
scripts/generate_sentinel_workbook.py (never hand-edit the JSON); it was
authored without a live workspace, so its README lists known blind spots.
The v2 ACME sample files (159 rules, 40 assets, 29 log sources) were added
2026-09-08 for a management demo by scripts/generate_uploadsample.py v2. The
three docs/ files are from the 2026-09-12 planning session.

Commits, in this order, each with a message body of 2-4 lines:
1. `apps/web/app/privacy/page.tsx` + `apps/web/components/MarketingFooter.tsx`
   + add `/privacy` to `apps/web/app/sitemap.ts`. Run `cd apps/web && npx tsc
   --noEmit` first. Message: "Marketing: privacy policy page and footer link".
2. `marketplace/sentinel/README.md`, `marketplace/sentinel/
   ScopeWiseMitreCoverage.workbook.json`, `scripts/generate_sentinel_workbook.py`.
   Before committing, regenerate with `python scripts/generate_sentinel_workbook.py`
   and confirm the JSON is byte-identical to what is on disk (if not, commit
   the regenerated one and say so). Message: "Sentinel Content Hub Phase A:
   generated MITRE coverage workbook v0 (untested against a live workspace)".
3. `scripts/generate_uploadsample.py`, `docs/sample/MITRE_Sample/UploadSample/
   README.md`, `acme_environment_v2.xlsx`, `acme_sentinel_usecases_v2.xlsx`.
   Run the generator once (`cd apps/api && python ../../scripts/
   generate_uploadsample.py v2`) to confirm it still hard-fails cleanly on
   nothing and the xlsx files match. Confirm no file under docs/sample/project/
   is staged (customer data, gitignored; check `git status --ignored` shows
   them ignored). Message: "MITRE samples: v2 ACME pair for management demo
   (159 rules, 40 assets, 29 log sources)".
4. `docs/planning/HOMEPAGE_BRANDING_SEO_PLAN.md`, `docs/phases/prompts/
   HOMEPAGE_BRANDING_SEO_PROMPT.md`, `docs/phases/prompts/
   CONTENT_PHASE_BLOG_COMPARE_PROMPT.md`, `docs/phases/prompts/
   PENDING_WORK_RUNBOOK_2026_09_12.md`, and the two modified index files
   `docs/IMPLEMENTATION_PROGRESS.md`, `docs/planning/SEO_STRATEGY.md`. Stage
   these by name, never `git add docs/`. Message: "Docs: homepage branding +
   SEO plan, kickoff prompts, pending-work runbook".

Secrets gate before each commit: grep the staged diff for the patterns in the
global playbook Phase 2 (AWS/OpenAI/GitHub/Google/Slack key shapes and
hardcoded password/secret/token literals). Report the four commit hashes and
`git status --short` (should be empty).
```

## Session 2 — Homepage branding + SEO build

Run the prompt in `docs/phases/prompts/HOMEPAGE_BRANDING_SEO_PROMPT.md`.
**You** will be asked for: the blog author name and title.

## Session 3 — Content phase

Run the prompt in `docs/phases/prompts/CONTENT_PHASE_BLOG_COMPARE_PROMPT.md`.
**You** will be asked for: the legal entity name for the LICENSE file, and a
read-through of the four posts before `pendingReview` is flipped.

---

## Session 4 — Push, deploy, smoke test

```
Read CLAUDE.md fully (Git / deployment and VPS sections are binding), then the
two newest files in docs/phases/summaries/. The homepage and content sessions
have landed locally as a series of commits on master. I am explicitly
authorising push and deploy in this session.

1. `git log origin/master..HEAD --oneline` and list what will be pushed. Run
   `cd apps/web && npx tsc --noEmit` and `next build` once more. Grep the
   marketing tree for "EDGP" and for any statistic not in
   docs/planning/HOMEPAGE_BRANDING_SEO_PLAN.md §5.
2. `git push`. If rejected for email privacy, use the amend pattern in the
   global playbook (env-var committer override + --author), never git config.
3. Deploy exactly per CLAUDE.md: ssh a11yos-vps, cd /opt/scopewise, git pull,
   docker compose -f docker-compose.vps.yml --env-file .env build, then
   GIT_SHA=$(git rev-parse --short HEAD) docker compose ... up -d. Only
   scopewise-* containers. No new migrations are expected; confirm by
   checking apps/api/migrations for anything newer than 039 and stop and tell
   me if there is.
4. Prod Python 3.11 smoke: run the in-container compile check from memory
   `prod-python-311-fstring-gotcha` even though this deploy is frontend-only.
5. Smoke test with a Haiku subagent returning a checkmark table: HTTP 200 and
   correct <title> on /, /product/sow-review, /product/mitre-coverage,
   /product/code-security-review, /pricing, /privacy, /terms,
   /compare/scopewise-vs-manual-review, /resources/blog; 301 from /product;
   robots.txt disallows /mitre /codereview /admin /login /api and not
   /pricing; sitemap.xml contains the new pages and none of the pendingReview
   posts; og:image resolves 200; Lighthouse SEO and Accessibility on / and
   one product page against the live URL.
6. Google Search Console: tell me the exact steps to resubmit the sitemap and
   request indexing for the three product pages (I do it; you do not have
   dashboard access). Same for the GA4 event names added for product-card
   clicks.
7. Session exit per CLAUDE.md: IMPLEMENTATION_PROGRESS entry, handoff summary
   with commit list and agent-utilization footer. Report deployed SHA, smoke
   table, and anything that failed verbatim.
```

## Session 5 — Core Web Vitals re-check + templates lead magnet

```
Read CLAUDE.md, docs/planning/seo/IMPLEMENTATION_ROADMAP.md Phase 3 (the two
open technical items: CWV re-check and /resources/templates), and
docs/planning/HOMEPAGE_BRANDING_SEO_PLAN.md §6. The homepage rebuild added
Inter via next/font and an OG image; this session measures the cost and
ships the one remaining Phase 3 technical item.

Part A, measure first: run Lighthouse (mobile and desktop) against the live
site for /, /product/sow-review, /product/mitre-coverage, /resources/blog
and one blog post. Compare with the Phase 1 baseline recorded in the roadmap
and the 2026-07-20 handoff. Report LCP, INP, CLS, TBT per page in a table. Fix
only real regressions, in this preference order: image sizing/priority on
the hero, font subsetting and preload, deferring the GA4 script, removing
unused client components from marketing pages. No new dependencies. Re-measure
and show before/after.

Part B, templates lead magnet, laziest working version: a single
/resources/templates page listing 3 downloadable files (SOW review checklist
PDF, RFP evaluation criteria worksheet XLSX, MITRE environment inventory
template XLSX; the last already exists via the MITRE template endpoint, reuse
it, do not duplicate). Gate: name + work email form that POSTs to the
existing contact endpoint (apps/api/app/... find the /api/v1/contact handler
the contact form already uses) with a `source: templates` field, then reveals
the download links client-side. No new tables, no email service, no CRM.
Honeypot field + the existing rate limiting. Privacy page gets one sentence
about what the form collects. `FAQPage` not needed; `WebPage` + `Dataset` schema
not needed either; keep it plain. Add to sitemap, footer Resources.

Constraints: tsc clean, Lighthouse Accessibility >= 95 on the new page, no
change to apps/api beyond reading the contact handler (if it cannot accept an
extra field, tell me rather than modifying it; a one-line optional field is
acceptable if it is already a free-form payload). Do not push or deploy; I
will run Session 4's steps again when I want it live.

Exit: tick the two roadmap items, IMPLEMENTATION_PROGRESS entry, handoff
summary with agent-utilization footer.
```

## Session 6 — Legal severity calibration pack (prep for the SME)

```
Read CLAUDE.md, docs/planning/LEGAL_SEVERITY_CALIBRATION.md (all sections;
§4 is the unchecked done-list), docs/planning/SCORING_METHODOLOGY.md §Finding
Severity, docs/planning/PROMPT_ENGINEERING_GUIDE.md "Real test set" section,
and docs/planning/ACCURACY_BASELINE_2026_07_22.md. Do not edit any agent
prompt in apps/api/app/ai/agent.py in this session.

Goal: produce everything a legal subject-matter expert needs to complete the
§3 worksheet in one sitting, so the sign-off stops being blocked on
preparation. No code changes to the product.

1. Pick the real (not synthetic) sample set: the four US federal contracts
   noted in the guide's "Real test set status" plus any commercial SOW under
   docs/sample/ that is public/non-customer (never docs/sample/project/). If
   fewer than 3 usable documents exist, say so and stop after step 2.
2. Run the current LegalReviewer over each document locally (use the existing
   review pipeline via the API against the local dev stack; check Docker is up
   and follow the edgp_test single-runner memory rule if tests are involved).
   Capture every Legal finding with: finding type, severity, confidence,
   quoted evidence, page/section anchor.
3. Build the SME pack as an XLSX under docs/planning/severity_calibration/
   (new folder): one row per finding, columns for the SME's rating
   (critical/major/medium/low), agree/disagree, and comment; a summary sheet
   with the severity distribution per document; a Read Me sheet explaining the
   four severity definitions from SCORING_METHODOLOGY.md verbatim and the
   30-minute procedure. Use openpyxl, reuse the styling helpers in
   apps/api/app/mitre/report_xlsx.py rather than writing new ones.
4. Write docs/planning/severity_calibration/README.md: how the pack was
   produced (git SHA, model used per the audit footer, date), how to ingest
   the SME's answers back (a short script that reads the returned XLSX and
   prints agreement rate per finding type and the systematic over/under
   patterns §4 asks about). Write that script as
   scripts/severity_calibration_ingest.py with one self-check.
5. Update LEGAL_SEVERITY_CALIBRATION.md: link the pack, keep the §4 boxes
   unchecked (only the SME's returned worksheet ticks them), add a "Pack
   generated <date>" line.

Constraints: use the OpenRouter key already configured in apps/api (memory
`openrouter-key-identity`), never the personal tooling key; expect a few
dollars of spend at most and tell me the total from the audit footers.
Stage files by name, never git add docs/ wholesale, commit once, do not push.
Report: documents used, finding count per document, spend, and the one thing
I need to do (send the pack to the SME).
```

## Session 7 — Sentinel workbook first live test

```
Prerequisite from me: a Sentinel workspace (trial is fine) that I can paste a
workbook into, and I will run the paste steps myself since you have no Azure
portal access. Your job is to drive the iteration loop.

Read docs/planning/SENTINEL_CONTENT_HUB_ADDON_PLAN.md Phase A and
marketplace/sentinel/README.md, especially "Known blind spots to verify in
the first live test". The workbook JSON is generated by
scripts/generate_sentinel_workbook.py; never hand-edit the JSON, always fix
the generator and regenerate.

Loop: I paste the current JSON into Sentinel Advanced Editor and report back
what each of the five tabs shows (or the error text). For each failure you
(a) locate the generator code that emits that block, (b) propose the minimal
fix with the exact ARM/KQL/JSONPath change and why, (c) regenerate, (d) give
me the new JSON path to paste. Keep a running table in the README of blind
spot -> observed -> fix -> verified. Test order: empty workspace first, then
a workspace with rules. When all five tabs render on both, tick Phase A step
3 in the plan, bump a version string in the generator, commit once with the
generator + JSON + README, do not push.

Constraints: no changes outside marketplace/sentinel/ and
scripts/generate_sentinel_workbook.py; the workbook must keep the "nothing
leaves the tenant" property (no external URLs except the ScopeWise link on
the last tab); no credentials anywhere in the JSON.
```

---

## Items with no prompt (owner-side)

- **Partner Center business verification** (Foxfiber Retail LLP, REG-06,
  attempt 2/3): check status; Phase B packaging waits on Phase A live test.
- **Splunk connector first live pull**: needs a reachable customer Splunk.
  When one exists, run the ops runbook in MITRE_MODULE_REFERENCE.md.
- **First case study**: needs a customer's written consent; do not fabricate.
- **`/compare/[competitor]` pages**: legal sign-off on claims first.
