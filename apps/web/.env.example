NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME=EDGP
NEXT_PUBLIC_APP_VERSION=1.0.0

# Google Sign-In (same Client ID as the backend's GOOGLE_CLIENT_ID). The
# login page renders no Google button at all if this is unset.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
